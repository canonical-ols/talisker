# these are pinned for repeatability, but updated by pyup.io to be latest
pytest==4.6.11  # pyup: <5.0.0
freezegun==0.3.15
pytest-cov==2.10.0
pytest-postgresql==2.4.0  # pypup: <2.0
pytest-xdist==1.33.0
mirakuru==1.1  # pyup: <2.0
pytest-timeout==1.4.1
responses==0.10.15  # pypp: <0.10.9
# setuptools 45+ does do py2
setuptools==44.1.1  # pyup: <45.0.0
coverage==5.2
flaky==3.6.1

# for integration tests
eventlet==0.25.1
gevent==1.5.0
# the bottom pin is for limbo test runs, as latest version doesn't work with
# newer celery versions
redis>=2.10.6,<=3.2.1
SQLAlchemy==1.3.13
